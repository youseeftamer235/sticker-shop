import {notFound} from 'next/navigation';import {categories,products} from '@/lib/data';import ProductCard from '@/components/ProductCard';
export default async function Category({params}:{params:Promise<{category:string}>}){const {category}=await params;const c=categories.find(x=>x.slug===category);if(!c)return notFound();return <main className="mx-auto max-w-7xl px-4 py-12"><div className="mb-10"><div className="text-5xl">{c.emoji}</div><h1 className="mt-3 text-4xl font-black">{c.name}</h1></div><div className="grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">{products.filter(p=>p.category===category).map(p=><ProductCard key={p.id} product={p}/>)}</div></main>}
