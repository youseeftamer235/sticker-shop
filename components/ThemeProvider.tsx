'use client';import {useEffect,useState,ReactNode} from 'react';
export default function ThemeProvider({children}:{children:ReactNode}){const [dark,setDark]=useState(false);useEffect(()=>{const saved=localStorage.getItem('theme');const d=saved==='dark'||(!saved&&matchMedia('(prefers-color-scheme: dark)').matches);setDark(d);document.documentElement.classList.toggle('dark',d)},[]);useEffect(()=>{document.documentElement.classList.toggle('dark',dark);localStorage.setItem('theme',dark?'dark':'light')},[dark]);return <>{children}<button aria-label="toggle theme" onClick={()=>setDark(v=>!v)} className="hidden" id="theme-controller"/></>}
